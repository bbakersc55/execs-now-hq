[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts = --strict-markers -q
testpaths = tests
markers =
    gcs_live: talks to the real gs://execs-now-hq-media bucket; runs only with RUN_GCS_LIVE=1
