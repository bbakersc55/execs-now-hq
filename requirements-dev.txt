# Development and test dependencies, fully pinned.
#
#     .venv/bin/pip install -r requirements-dev.txt
#
# pytest + pytest-django + factory_boy back the two mandatory test families
# (assumption B3). Everything below the direct three is transitive.

-r requirements.txt

factory_boy==3.3.3
Faker==40.38.0
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
pytest-django==4.14.0
